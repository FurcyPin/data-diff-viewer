/*
The db_base64 is a placeholder that will contain the diff report data.
The data is written as duckdb database file encoded in base64.
The Python code of data_diff_viewer will replace the {{db_base64}} string with the real encoded data.
*/
const db_base64 = `{{db_base64}}`;
export default db_base64 ;
